import React from 'react';
import {Text, View} from 'react-native';

const DynamicFormScreen: React.FC = () => {
  return (
    <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
      <Text>DynamicForm!</Text>
    </View>
  );
};

export default DynamicFormScreen;
